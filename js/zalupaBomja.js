const button = document.getElementById("button-container"); // находим кнопку для открытия/закрытия окна навигации
const outside = document.getElementById("particles-container"); // находим окно навигации

outside.addEventListener("click", () => {
    alert("ты че, ебанатище косорылое, попасть по кнопке не можешь?)");
});
